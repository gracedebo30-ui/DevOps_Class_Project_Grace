receivers:
- name: "slack"
  slack_configs:
  - channel: "devops"
    send_resolved: true
    api_url:https://example.com/fake-url-untuk-tugas
    title: "{{ .CommonAnnotations.description }}"
    text: "Description: {{ .CommonAnnotations.description }}"
route:
  routes:
  - match:
      alertname: TestPodNotRunning
    receiver: "slack"